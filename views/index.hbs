<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>App</title>
  </head>
  <body>
    <img style="max-width: 200px; max-height: 400px; object-fit:cover;" src="http://localhost:3000/static/images/{{message.img}}" alt="">
    <h1>Title:{{message.name}}</h1>
    <p>Description: {{message.description}}</p>
    <p>Rating: {{message.rating}}</p>
  </body>
</html>